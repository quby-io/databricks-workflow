{
  "active_jobs": ["initialize", "create_features", "train_presence_model", "monitor_presence_model"],
  "config": {
    "settings": {
      "notebook_task": {
        "base_parameters": {
          "rawDb": "staging",
          "featureDb": "staging_features"
        }
      },
      "new_cluster": {
        "node_type_id": "m5.large",
        "num_workers": 1
      },
      "email_notifications": {
        "on_failure": [
          ""
        ]
      }
    }
  }
}
